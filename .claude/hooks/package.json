{
  "name": "claude-hooks",
  "version": "1.0.0",
  "description": "Claude Code hooks for Gefjon Growth hiring pipeline",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
